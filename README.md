# ImageDrawingConverter (WIP)
This program takes an input image and converts it to a series of lines that a robot can then use to draw the image. It's like an image - to painting coordinates converter.
